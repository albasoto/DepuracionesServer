/**
 * ColumnaController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `ColumnaController.id()`
   */
  id: function (req, res) {
    return res.json({
      todo: 'id() is not implemented yet!'
    });
  },

  /**
   * `ColumnaController.nombre()`
   */
  nombre: function (req, res) {
    return res.json({
      todo: 'nombre() is not implemented yet!'
    });
  },

  /**
   * `ColumnaController.tamanio()`
   */
  tamanio: function (req, res) {
    return res.json({
      todo: 'tamanio() is not implemented yet!'
    });
  },

  /**
   * `ColumnaController.tipoColumna()`
   */
  tipoColumna: function (req, res) {
    return res.json({
      todo: 'tipoColumna() is not implemented yet!'
    });
  }

};

